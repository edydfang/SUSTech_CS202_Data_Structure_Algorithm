#include <bits/stdc++.h>

using namespace std;

int main(){
    int T,i =0, n;
    long m;
    cin >> total;
    for(i=0;i<T;i++){
        cin >> n >> m;
        long numbers[n];
        long needs[n]
        memset(numbers,0,sizeof(numbers));
        for(j=0;j<n;j++){
            cin >> numbers[j];
        }

        // quick sort needs[] and 
        // search
    }
}